import React from 'react';

import {View, StyleSheet} from 'react-native';

const SizedWidth = () => {
  return <View style={styles.style} />;
};

const styles = StyleSheet.create({
  style: {
    width: 25,
  },
});
export default SizedWidth;
